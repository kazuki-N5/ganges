# 疑似配達ライブアニメーションマップ機能 仕様書・構造設計

商品を購入した後に「今どこまで届いているか」を**マップ上でリアルタイムに走行するトラック・配達員アニメーション**で眺めてワクワクし、届いた瞬間の感動と満足感を疑似体験するための『疑似配達ライブマップ機能』の構造設計ドキュメントです。

---

## 1. コンセプトとワクワク体験設計

1. **ボトムバーの「配達」タブからアクセス！**
   - 注文詳細を個別に開くのではなく、アプリの**ボトムナビゲーションバー（`BottomNavigationBar`）に「配達
     🚚」タブ**を配置。
   - タブをタップすると、画面中央に**1台の大きなリアルタイムライブマップ**が展開されます。

2. **複数購入商品が1つのマップ上に集合！自宅（📍）へ一斉に近づくアニメーション**
   - マップの中央・目的地には**「自宅ピン（📍）」**が配置されます。
   - 購入した各商品がそれぞれの発送拠点（商品IDのハッシュから割り当て）から、**点線ルート（点点点点...）を描きながら1つのマップ上に同時に配置**されます。
   - 全ての商品・荷物アイコン（📦1, 📦2,
     🚚）が、時間の経過とともに**点線ルートに沿ってリアルタイムで自宅ピン（📍）に向かって徐々に近づいてくる**ダイナミックな走行アニメーションを実現します。

3. **インタラクティブなアニメーション＆完了演出**
   - **配送ルート点線（Polyline）**: 各拠点 ➔ 中継デポ ➔
     自宅を結ぶ複数のルートがマップ上に点線描画。
   - **トラック・荷物の同時走行アニメーション**: `AnimationController` /
     `CustomPainter` により、複数荷物の現在地座標を同期更新。
   - **ピンポン＆置配モーダル**:
     トラックが自宅に到達した順に、ピンポーン♪の効果音演出とともに「玄関前への置き配が完了しました！📸
     (置配写真)」という達成感モーダルがスライドイン！

---

## 2. 疑似配達の進行ステータスとマップアニメーションの連動

| ステータス                | 各荷物のマップ上位置       | マップ上のアニメーション演出                                           | 経過時間モデル |
| :------------------------ | :------------------------- | :--------------------------------------------------------------------- | :------------- |
| **1. 注文受付完了**       | 発送拠点 (各FC/デポ)       | 各発送拠点ピンが点滅、出荷ラベル印刷アニメーション                     | 0秒〜10秒      |
| **2. 出荷・一斉発送済み** | 拠点 ➔ 高速道路            | 複数の荷物・トラックアイコン（📦🚚）が点線ルートに沿って一斉走行開始   | 10秒〜30秒     |
| **3. 配送デポ通過**       | 地域中継デポ               | 中継デポを通過、ラストワンマイルの配達員アイコンへバトンタッチ         | 30秒〜60秒     |
| **4. 配達中 (接近中！)**  | 自宅周辺の道路 (あと数百m) | 複数の点線ルートが自宅周辺で合流！住宅街をカーブしながら自宅へ同時接近 | 60秒〜90秒     |
| **5. 順次配達完了！**     | ご自宅前 (📍)              | 自宅に到達した荷物から「配達完了！📦」のお祝いエフェクトと置配完了通知 | 90秒 (1分30秒) |

---

## 3. マップアニメーション描画・技術構造設計

### ① 画面構成 (`DeliveryLiveMapScreen`)

アプリの**ボトムバー「配達」タブ**から直接画面を開き、1つのマップ上に複数商品の配送ルートとアプローチ状況がリアルタイム描画されます。

```text
+--------------------------------------------------+
|  📦 リアルタイム配達ライブマップ                   |
+--------------------------------------------------+
|                                                  |
|       🏢 (市川FC) ─ ─ 📦1 ─ ─ ┐                  |
|                               │                  |
|       🏢 (川崎FC) ─ ─ 📦2 ─ ─ ┼ ─ ─ 📍(自宅)     |
|                               │     ▲            |
|       🏢 (大阪FC) ─ ─ ─ ─ 📦3 ┘     │            |
|                           (徐々に自宅へ接近中)   |
|                                                  |
+--------------------------------------------------+
| 📦 [商品1] ダイナミックマイク                      |
|    └ 到着まで: あと 30 秒 (距離: 200m)            |
| 📦 [商品2] ワイヤレスイヤホン                      |
|    └ 到着まで: あと 1 分 10 秒 (距離: 550m)        |
| 📦 [商品3] 天然シリカ猫砂                          |
|    └ 到着まで: あと 2 分 15 秒 (距離: 1.2km)       |
+--------------------------------------------------+
|  [ホーム]   [検索]   [カート]   [配達🚚]  [マイページ] |
+--------------------------------------------------+
```

### ② マップ＆アニメーション実装ロジック (Flutter)

```dart
// ルート軌跡上の座標計算とアニメーション制御
class DeliveryMapPainter extends CustomPainter {
  final double progress; // 0.0 ~ 1.0 (配送進捗率)
  final List<Offset> routePoints; // マップ上の配送ルート座標リスト

  DeliveryMapPainter({required this.progress, required this.routePoints});

  @override
  void paint(Canvas canvas, Size size) {
    // 1. 背景・道路・建物の描画
    _drawMapBackground(canvas, size);

    // 2. 配送ルート点線（Polyline）の描画
    _drawRouteLine(canvas, routePoints);

    // 3. 進捗率(progress)に応じたトラックの現在座標(Offset)計算
    final currentPos = _calculateCurrentPosition(routePoints, progress);

    // 4. トラックアイコン(🚚)および自車位置ピンの描画
    _drawTruckIcon(canvas, currentPos);
    _drawHomePin(canvas, routePoints.last);
  }

  @override
  bool shouldRepaint(covariant DeliveryMapPainter oldDelegate) {
    return oldDelegate.progress != progress;
  }
}
```

---

## 4. 発送元拠点（スタート地点）の決定アルゴリズム

APIからは直接の配送倉庫の座標データは取得できないため、商品ID（`itemCode`）のハッシュ値を利用して「リアルで納得感のある発送元拠点」を動的に一意生成します。

### 商品ID（`itemCode`）のハッシュ値連動（固定でリアル）

- `itemCode`
  のハッシュコードから、全国の主要大型物流センター（Amazon市川FC、川崎FC、八王子FC、大阪茨木FC、鳥栖FCなど）を1つ一意に自動割り当て。
- ※同じ商品を購入した場合、毎回必ず同じ発送元から届くためリアリティが生まれます。

#### 💡 発送拠点算出の実装コード例 (Dart)

```dart
// 発送拠点の定義モデル
class DeliveryHub {
  final String name;       // 拠点名 (例: Amazon市川FC)
  final double latitude;   // 緯度
  final double longitude;  // 経度

  const DeliveryHub({
    required this.name,
    required this.latitude,
    required this.longitude,
  });
}

// 拠点決定サービス・ユーティリティ
class DeliveryHubResolver {
  // 全国の主要物流センターリスト（マスターデータ）
  static const List<DeliveryHub> _hubs = [
    DeliveryHub(name: 'Amazon市川FC (千葉)', latitude: 35.6882, longitude: 139.9238),
    DeliveryHub(name: 'Amazon川崎FC (神奈川)', latitude: 35.5308, longitude: 139.7029),
    DeliveryHub(name: 'Amazon八王子FC (東京)', latitude: 35.6558, longitude: 139.3239),
    DeliveryHub(name: 'Amazon大阪茨木FC (大阪)', latitude: 34.8164, longitude: 135.5684),
    DeliveryHub(name: 'Amazon鳥栖FC (佐賀)', latitude: 33.3762, longitude: 130.5098),
    DeliveryHub(name: '名古屋デポ (愛知)', latitude: 35.1814, longitude: 136.9064),
    DeliveryHub(name: '仙台物流センター (宮城)', latitude: 38.2682, longitude: 140.8694),
  ];

  /// itemCode のハッシュ値から、常に同じ発送拠点を一意に決定する
  static DeliveryHub resolveFromItemCode(String itemCode) {
    if (itemCode.isEmpty) {
      return _hubs.first;
    }
    // itemCodeの文字列ハッシュ値を正の整数に変換
    final int hashCode = itemCode.hashCode.abs();
    // 余り計算(%)でリストから一意に1つの拠点を選択
    final int index = hashCode % _hubs.length;
    return _hubs[index];
  }
}
```

---

## 5. データ構造・データベース設計 (SQLite / Dart)

### `orders` テーブルへの拡張フィールド

```sql
ALTER TABLE orders ADD COLUMN tracking_number TEXT;          -- 追跡番号
ALTER TABLE orders ADD COLUMN delivery_status TEXT DEFAULT 'ordered'; -- ステータス
ALTER TABLE orders ADD COLUMN start_hub_name TEXT;           -- 発送元拠点名 (例: Amazon市川FC)
ALTER TABLE orders ADD COLUMN current_latitude REAL;         -- 現在のトラック緯度(擬似)
ALTER TABLE orders ADD COLUMN current_longitude REAL;        -- 現在のトラック経度(擬似)
ALTER TABLE orders ADD COLUMN updated_at TEXT;
```

---

## 6. ディレクトリ構造 (`lib/features/delivery/`)

```text
lib/
└── features/
    └── delivery/                 # 疑似配達ライブマップモジュール
        ├── domain/
        │   └── delivery_status_model.dart # 配送ステップ・座標モデル
        ├── repository/
        │   └── delivery_repository.dart   # 発送元算出・リアルタイムタイマー制御
        ├── providers/
        │   └── delivery_provider.dart     # 進捗率(0.0~1.0)アニメーションNotifier
        └── presentation/
            ├── delivery_live_map_screen.dart # ライブ配送マップメイン画面
            └── widgets/
                ├── delivery_map_canvas.dart  # CustomPainter動的マップ描画
                └── delivery_status_card.dart # 下部カウントダウン・ドライバーカード
```
