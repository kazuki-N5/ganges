# 楽天商品検索API（新仕様）対応仕様書

本ドキュメントは、Ganges アプリにおける **楽天市場商品検索 API (Rakuten Ichiba Item Search API)** のリアルデータ連携に関する仕様、設定手順、画面別のデータ取得フロー、および実装方針をまとめた仕様書です。

---

## 1. 概要

従来使用されていたモックデータから、本物の楽天市場の商品情報（商品名、価格、画像URL、ショップ名、アフィリエイトURL等）を取得するリアルAPI通信へ移行します。

---

## 2. 楽天API新仕様と接続要件

2026年の楽天ウェブサービス（Rakuten Developers）仕様改定に伴い、以下の接続要件が必須となります。

| 項目 | 旧仕様 | 新仕様（現在） | 備考 |
| :--- | :--- | :--- | :--- |
| **APIドメイン** | `app.rakuten.co.jp` | **`openapi.rakuten.co.jp`** | ドメインが完全移行 |
| **エンドポイントURL** | `/services/api/IchibaItem/Search/20220601` | **`/ichibams/api/IchibaItem/Search/20260701`** | パスおよびバージョン変更 |
| **認証キー** | `applicationId` のみ | **`applicationId` + `accessKey`** | `pk_` で始まるアクセスキーが必須 |
| **セキュリティ検証** | 特になし | **`Origin` / `Referer` ヘッダー検証** | ポータルで登録したドメインと一致が必要 |

---

## 3. 環境変数 (`.env`) 設定

アプリのルート直下にある `.env` ファイルに以下の設定を定義します。

```env
RAKUTEN_APP_ID=281b03ba-0b0d-4dd2-ad1b-c18591876a57
RAKUTEN_ACCESS_KEY=pk_GsA5LStwbrlLd7weFnKO7vKZmwgIRJA1q9I5o4KDBM4
RAKUTEN_AFFILIATE_ID=563dc8f2.5cdefd3e.563dc8f3.909beb41
```

* ※ `RAKUTEN_ACCESS_KEY`: Rakuten Developers で取得したアクセスキー。
* ※ Rakuten Developers の「Allowed websites」には `example.com` を登録済み。

---

## 4. API疎通テスト結果

以下のリクエスト条件で API 疎通テストを実施し、正常なレスポンス（200 OK）が得られることを検証・確認済みです。

### リクエスト条件
* **URL**: `https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260701`
* **HTTPヘッダー**: `Origin: https://example.com`
* **パラメータ**:
  * `applicationId`: `281b03ba-0b0d-4dd2-ad1b-c18591876a57`
  * `accessKey`: `pk_GsA5LStwbrlLd7weFnKO7vKZmwgIRJA1q9I5o4KDBM4`
  * `affiliateId`: `563dc8f2.5cdefd3e.563dc8f3.909beb41`
  * `format`: `json`
  * `keyword`: `iPhone`

### レスポンスデータ例（一部抜粋）
```json
{
  "itemName": "未使用〜中古 iPhone16 Pro Max 256GB/512GB/1TB SIMフリー",
  "itemPrice": 179800,
  "shopName": "ダイワンテレコム楽天市場店",
  "itemUrl": "https://hb.afl.rakuten.co.jp/...",
  "mediumImageUrls": [
    { "imageUrl": "https://thumbnail.image.rakuten.co.jp/@0_mall/dai-one/cabinet/sku_top/iphone16promax.jpg?_ex=128x128" }
  ]
}
```

---

## 5. 画面別のデータ取得タイミングと処理フロー

アプリ内の各画面やユーザーのアクションにおいて、**「どこで」「いつ」「どのように」**楽天APIを呼び出してデータを取得するかの詳細定義です。

```
 [ユーザー操作]              [Riverpod Provider]           [RakutenApiClient]         [楽天API (openapi)]
   │                             │                              │                             │
   ├── ①ホーム画面起動 ─────────> productListNotifierProvider ──> getRankingProducts() ─────────> 商品ランキングAPI通信
   │                             │                              │                             │
   ├── ②カテゴリタブ選択 ────────> categoryFilterProvider ──────> searchProducts('家電'等) ───> 商品検索API通信
   │                             │                              │                             │
   └── ③検索窓キーワード検索 ───> searchNotifierProvider ───────> searchProducts(入力ワード) ───> 商品検索API通信
```

### ① ホーム画面表示時 (初回ロード)
* **タイミング**: アプリ起動時およびホームタブ (`HomeScreen`) が初めて描画された時。
* **データ取得方針**:
  * 特定のキーワードやジャンル/プロジェクト等の指定不要で、今最も売れている・注目されている「あなたへのおすすめ」「トレンド商品」を取得するため、**「楽天市場商品ランキングAPI (IchibaItem Ranking API)」** を利用します。
  * エンドポイント: `https://app.rakuten.co.jp/services/api/IchibaItem/Ranking/20220601`（または新openapi仕様）
  * デフォルト条件: ジャンルID等の指定なし（総合ランキング上位）、取得件数 `hits`: 30件
* **処理フロー**:
  1. `HomeScreen` の `build()` または `initState()` で Riverpod の `productListNotifierProvider` を読み込み。
  2. `ProductRepository.getRankingProducts()` を実行。
  3. `RakutenApiClient.getRankingProducts()` が楽天市場商品ランキングAPIへリクエストを行い、総合ランキング（おすすめ商品）の JSON を取得して `Product` モデルクラスのリストへ変換。
  4. ホーム画面のグリッドビュー / リストビューに「おすすめ・ランキング商品」としてリアルタイム描画。

### ② カテゴリタグ切り替え時
* **タイミング**: ホーム画面上部のカテゴリタグ（例:「すべて」「家電・PC」「ファッション」「コスメ」等）をユーザーがタップした時。
* **処理フロー**:
  1. タップされたカテゴリに対応する検索キーワード（例：「家電」「ファッション」など）を特定。
  2. `productListNotifierProvider` の `fetchCategory(categoryKeyword)` を実行。
  3. `RakutenApiClient.searchProducts(keyword: categoryKeyword)` を呼び出して最新一覧を取得。
  4. ホーム画面の表示リストを新しい検索結果で自動更新。

### ③ 検索バーからのキーワード検索時
* **タイミング**: ユーザーが検索バーに文字を入力し、キーボードの「検索」ボタンまたは検索アイコンを押した時。
* **処理フロー**:
  1. 検索入力値（例: `「完全ワイヤレスイヤホン」`）を取得。
  2. `searchNotifierProvider.search(query)` を実行。
  3. `RakutenApiClient.searchProducts(keyword: query, hits: 30, page: 1)` を呼び出し。
  4. 検索結果一覧画面へ結果を渡して表示。
  5. スクロールによる追加読み込み（無限スクロール）時は `page` パラメータをインクリメント（`page++`）して続きのデータを取得。

### ④ 商品詳細画面の表示時
* **タイミング**: 商品一覧から特定の「商品カード」をユーザーがタップした時。
* **処理フロー**:
  1. 一覧取得時に既に保持している `Product` オブジェクトを `ProductDetailScreen` の引数として受け渡して即座に高速表示。
  2. （※必要に応じて）アフィリエイト購入ボタンタップ時には、`Product` 内の `itemUrl`（楽天アフィリエイトリンク）を開く処理を実行。

---

## 6. Flutterアプリ側の実装改修手順 ([rakuten_api_client.dart](file:///c:/Users/kazuk/program/AppList/ganges/ganges/lib/core/api/rakuten_api_client.dart))

1. **環境変数の読み込み**
   * `dotenv.env['RAKUTEN_ACCESS_KEY']` を追加読み込み。
2. **ベースURLの更新**
   * `https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260701` へ変更。
3. **Dioヘッダーとクエリの更新**
   * リクエストヘッダーに `'Origin': 'https://example.com'` を追加。
   * クエリパラメータに `'accessKey': _accessKey` を追加。
4. **モックフラグの切り替え**
   * `useMockOnly` フラグを `false` に変更（実際の通信を有効化）。
